{
  "name": "strav",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter='*' build",
    "test": "bun run --filter='*' test",
    "publish-all": "bun run --filter='*' publish"
  },
  "devDependencies": {
    "bun": "latest"
  }
}